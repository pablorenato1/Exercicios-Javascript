/*Escreva uma função que receba dois números, sendo o primeiro a base
e o segundo é a potência.
Retornar o resultado */

function potencia(n,y) {
    return console.log(Math.pow(n,y));
}

potencia(2,2);
potencia(3,2);
potencia(2,10);