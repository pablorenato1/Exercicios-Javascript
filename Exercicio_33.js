/*Crie um JSON com 3 propriedades
Atribua a uma variavel
Mostrar no console as propriedades
*/

let person = {
    "name": "Pablo",
    "age": 23,
    "role": "Programador" 
}

console.log(person.name);
console.log(person.age);
console.log(person.role);