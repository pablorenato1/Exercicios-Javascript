let velocidade = 80;

//Velocidade max = 80
if (velocidade > 80) {
    console.log("Você está acima da velocidade permitida");
} else {
    console.log("Você não está acima da velocidade permitida");
}